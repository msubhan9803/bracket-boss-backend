export enum LevelTypeOrderNumber {
    pool_play = 1,
    play_off = 2
}

export enum LevelStatusTypesEnum {
    not_started = 'not_started',
    in_progress = 'in_progress',
    completed = 'completed'
}
